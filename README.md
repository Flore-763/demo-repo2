# Demo 2

Some text

## Github Workflow

Write code--> Commit changes --> Make a Pull Request

## Local Git Workflow

Write code --> Stage Changes(git add)-->Commit Changes(git commit)-->Push Changes(git push) -->Make a Pull Request

le fichier neeré a la chambre de commerce regroupant les entreprises dans chaque secteur d'activité mais payant!!!!!

## Some useful commandes in github

### git add .

### git commit -m "expliquer ce qui a été fait" -m "ajouter une description"

### git push -u origin main ou master selon le nom de la branche principale

### git clone lien ssh pour cloner un repos distant

### git init lien ssh pour joindre un repos local a un repo qui a ete cree sur le github

### git branch pour afficher toutes les branches disponibles

### git checkout -b nom de la nouvelle branche que l'on veut creer

### git status pour connaitre le statut de notre git , savoir s'il ya des commits ou des push a faire

### git branch -d flore-feature suppression de cette branche apres avoir fait un pull et un merge sur la branche principale

### on peut aussi faire un git diff sur les features pour voir ce qu'il ya sur cette branche et qui n'est pas encore dans la branche principale
